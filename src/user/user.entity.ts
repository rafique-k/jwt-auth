//this entity shows how user structure will look like and it can't be altered from anywhere else
export class User{
    username: string;
    password: string;
    email: string;
    age: number;
    role: string;
}